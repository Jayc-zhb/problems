from random import *
from cyaron import *

CASES = 20

for _t in range(1, CASES + 1):
    io = IO(f"{_t}.in")
    # ==============================
    io.input_writeln();
    # ==============================
    io.close()
